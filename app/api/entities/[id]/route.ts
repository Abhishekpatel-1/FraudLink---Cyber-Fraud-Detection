import {NextResponse} from 'next/server';import {prisma} from '@/lib/prisma';export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;const p=await prisma.phone.findUnique({where:{id},include:{imei:true,imsi:true,ips:true,person:true}});if(p)return NextResponse.json({type:'Phone',...p});const a=await prisma.bankAccount.findUnique({where:{id},include:{upi:true,transactionsFrom:true,transactionsTo:true,person:true}});if(a)return NextResponse.json({type:'Bank Account',...a});return NextResponse.json({error:'Entity not found'},{status:404})}
