import {NextResponse} from 'next/server';import {parseFile} from '@/lib/parsers';export const runtime='nodejs';export async function POST(req:Request){const fd=await req.formData();const f=fd.get('file');if(!(f instanceof File))return NextResponse.json({error:'file required'},{status:400});try{return NextResponse.json(await parseFile(f))}catch(e:any){return NextResponse.json({error:e.message},{status:400})}}
