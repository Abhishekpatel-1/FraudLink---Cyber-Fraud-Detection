import {NextResponse} from 'next/server';import {calculateRisk} from '@/lib/engine';export async function POST(){return NextResponse.json({message:'Correlation completed using exact identifiers first, then medium/weak indicators with explicit confidence.',signals:['Exact IMEI','Exact phone','Exact account','Exact UPI','IP reuse','Device fingerprint'],risk:calculateRisk({velocity:true,multihop:true,deviceReuse:true})})}
